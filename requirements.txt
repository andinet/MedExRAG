# LangChain VLM X-Ray Analysis Course - Dependencies
# Install with: pip install -r requirements.txt

# Core LangChain packages
langchain>=0.1.0
langchain-community>=0.1.0
langchain-core>=0.1.0
langgraph>=0.1.0
langsmith>=0.1.0

# Machine Learning & AI
transformers>=4.38.0
torch>=2.6.0  # CVE-2025-32434 requires >=2.6 for safe model loading
torchvision>=0.21.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # 4-bit quantization for low-VRAM GPUs
qwen-vl-utils>=0.0.1
huggingface-hub>=0.20.0

# Medical Imaging
pydicom>=2.4.0
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0

# Image Processing
scikit-image>=0.22.0
imageio>=2.33.0

# API & Web Framework
streamlit>=1.28.0  # For interactive web UI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Database & Caching
redis>=5.0.0
chromadb>=0.4.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
typing-extensions>=4.8.0
pydantic-settings>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # For testing FastAPI

# Logging & Monitoring
structlog>=23.2.0
sentry-sdk>=1.38.0
prometheus-client>=0.19.0  # Prometheus metrics (M2)

# OpenTelemetry Distributed Tracing (M3)
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-exporter-otlp>=1.21.0
opentelemetry-instrumentation>=0.42b0

# Documentation
mkdocs>=1.5.0
mkdocs-material>=9.4.0
mkdocstrings[python]>=0.24.0

# Development Tools
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0
ipython>=8.17.0
ipykernel>=6.27.0

# Jupyter
jupyter>=1.0.0
notebook>=7.0.0
ipywidgets>=8.1.0

# Optional: For production deployment (Unix/Linux only)
gunicorn>=21.2.0; sys_platform != 'win32'  # WSGI server (Unix only)
celery>=5.3.0; sys_platform != 'win32'     # Task queue (Unix only)
flower>=2.0.0; sys_platform != 'win32'     # Celery monitoring (Unix only)

# Optional: For enhanced functionality
sentence-transformers>=2.2.0  # For embeddings
faiss-cpu>=1.7.4  # Vector search (use faiss-gpu if you have CUDA)
pdfplumber>=0.10.0  # For PDF processing

# RAG System - DocLing for Advanced PDF Processing
docling>=1.0.0  # Advanced PDF parsing with structure preservation
docling-core>=1.0.0
docling-ibm-models>=1.0.0

# RAG System - Vector Store Integration
langchain-chroma>=0.1.0  # LangChain ChromaDB integration
pypdf>=3.17.0  # PDF utilities
pymupdf>=1.23.0  # Advanced PDF processing
python-magic>=0.4.27; sys_platform != 'win32'  # File type detection (Linux/Mac)
python-magic-bin>=0.4.14; sys_platform == 'win32'  # File type detection (Windows)

# Optional: For visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
