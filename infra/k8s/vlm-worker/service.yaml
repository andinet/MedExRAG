apiVersion: v1
kind: Service
metadata:
  name: vlm-worker
  namespace: medexrag
  labels:
    app: vlm-worker
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: vlm-worker
